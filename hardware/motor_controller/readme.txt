the motor controller will run the auralysSpeaker firmware and it is the same for ALL the motors in the system.

different settings for speed (and role) allows the software to change the behavior, but the firmware is the same on all devices.

